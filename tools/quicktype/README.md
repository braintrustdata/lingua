# Quicktype toolchain

This private workspace package pins the quicktype CLI used by Lingua's provider type generator.

The toolchain pins `quicktype@25.1.0` so provider type generation is reproducible locally and in CI.
