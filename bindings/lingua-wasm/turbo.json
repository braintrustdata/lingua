{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "../../Cargo.toml",
        "../../crates/**/src/**/*.rs",
        "../../crates/**/Cargo.toml"
      ],
      "outputs": ["nodejs/**", "web/**"]
    }
  }
}
