// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CacheControlEphemeral } from "./CacheControlEphemeral";
import type { Caller } from "./Caller";
import type { Citations } from "./Citations";
import type { InputContentBlockContent } from "./InputContentBlockContent";
import type { InputContentBlockType } from "./InputContentBlockType";
import type { RequestImageTransformations } from "./RequestImageTransformations";
import type { SourceUnion } from "./SourceUnion";

/**
 * Regular text content.
 *
 * Image content specified directly as base64 data or as a reference via a URL.
 *
 * Document content, either specified directly as base64 data, as text, or as a reference
 * via a URL.
 *
 * A search result block containing source, title, and content from search operations.
 *
 * A block specifying internal thinking by the model.
 *
 * A block specifying internal, redacted thinking by the model.
 *
 * A block indicating a tool use by the model.
 *
 * A block specifying the results of a tool use by the model.
 *
 * A content block that represents a file to be uploaded to the container
 * Files uploaded via this block will be available in the container's input directory.
 */
export type InputContentBlock = { 
/**
 * Create a cache control breakpoint at this content block.
 */
cache_control: CacheControlEphemeral | null, citations: Citations | null, text: string | null, type: InputContentBlockType, source: SourceUnion | null, 
/**
 * Configures the transformations the server applies to this image before the model observes
 * it. Each key names a condition the server transforms images for; its value selects the
 * transformation applied. Omitted keys keep their default behavior, and an empty object is
 * equivalent to omitting the field.
 */
transformations?: RequestImageTransformations | null, context: string | null, title: string | null, content: InputContentBlockContent | null, 
/**
 * The `signature` value of this thinking block, exactly as returned by the API in a
 * previous response. Used to verify that the block was generated by Claude.
 *
 * Thinking blocks must be passed back unmodified and in their original order; a modified
 * block results in a 400 `invalid_request_error`.
 */
signature: string | null, 
/**
 * The `thinking` text of this block as returned by the API.
 */
thinking: string | null, 
/**
 * The `data` value of this redacted thinking block, exactly as returned by the API in a
 * previous response. Opaque and encrypted; pass it back unchanged.
 */
data: string | null, caller: Caller | null, id: string | null, input: unknown, name: string | null, 
/**
 * For a toolset member tool_use, the toolset family this member belongs to.
 *
 * For a toolset member tool_result, the toolset family of the paired tool_use.
 */
toolset_name: string | null, is_error: boolean | null, tool_use_id: string | null, file_id: string | null, };
