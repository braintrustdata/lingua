// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BrowserStateChange } from "./BrowserStateChange";
import type { BrowserStateTabEntry } from "./BrowserStateTabEntry";
import type { CacheControlEphemeral } from "./CacheControlEphemeral";

/**
 * The caller's browser state after a browser toolset member call —
 * the full inventory of open tabs, which tab is active, and any side
 * effects (tabs opened, download state changes) the call produced.
 *
 * At most one per `tool_result`, only on a non-error result answering a
 * browser toolset member `tool_use`. The server renders the
 * model-visible text from it; the model never sees the raw fields.
 */
export type RequestBrowserStateBlock = { 
/**
 * Create a cache control breakpoint at this content block.
 */
cache_control: CacheControlEphemeral | null, 
/**
 * Tabs opened and download state changes during this call. "Nothing to report" is expressed by omitting the field, never by an empty list.
 */
state_changes: Array<BrowserStateChange> | null, 
/**
 * All tabs open in the browser after this call — the full inventory, not a delta. May be empty. Whenever non-empty, exactly one entry carries `active: true`.
 */
tabs: Array<BrowserStateTabEntry>, };
